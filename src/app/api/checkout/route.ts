import { NextRequest, NextResponse } from "next/server"; import { MercadoPagoConfig, Preference } from "mercadopago"; const client = new MercadoPagoConfig({ accessToken: process.env.MP_ACCESS_TOKEN || "" }); export async function POST(req: NextRequest) { try { const body = await req.json(); const { planId, planNome, valor, email } = body; const preference = new Preference(client); const response = await preference.create({ body: { items: [{ id: planId, title: "DMS FLOW - Plano " + planNome, quantity: 1, unit_price: Number(valor), currency_id: "BRL" }], payer: { email: email || "cliente@dmsflow.com.br" }, back_urls: { success: (process.env.APP_URL || "") + "/registro?pagamento=aprovado&plano=" + planId, failure: (process.env.APP_URL || "") + "/planos?pagamento=erro", pending: (process.env.APP_URL || "") + "/registro?pagamento=pendente&plano=" + planId }, auto_return: "approved", statement_descriptor: "DMS FLOW", external_reference: planId } }); return NextResponse.json({ checkoutUrl: response.init_point, preferenceId: response.id }); } catch (error) { console.error("Erro Mercado Pago:", error); return NextResponse.json({ error: "Erro ao criar pagamento" }, { status: 500 }); } }